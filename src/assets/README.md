# Notebook artwork

## Opening and closing states

`notebook-open-source.png` and `notebook-ajar-source.png` were edited with the built-in image generation tool using the closed notebook as the visual reference. Their matching `notebook-open.png` and `notebook-ajar.png` files are 256 × 256 runtime exports made with macOS `sips`.

Five states play once on entry (780 ms) and normal exit (500 ms): closed → strap lifted → strap slipped aside → half open → open, reversed on exit. Any key skips; signals bypass the closing sequence. The two strap states share the closed book's exact placement. Ordinary pages show the closed notebook at 4 columns × 2 rows beside the title. Operations lasting over 600 ms use the same opening sequence, up to 24 columns × 12 rows; progress animates independently. Terminals without an enabled image transport use the character sprites in `../notebook-pixels.mjs`, including the writing loop while busy. Small windows keep the text mark. Native PNGs remain the preferred artwork.

`notebook-strap-lift-source.png` and `notebook-strap-free-source.png` were edited from the closed reference with the built-in image generation tool. Matching runtime PNGs are 128 × 128 `sips` exports.

Strap-lift prompt:

> Edit ONLY the thin horizontal leather elastic strap on this exact closed notebook to make the first release animation frame. Preserve absolutely the notebook silhouette, size, screen position, perspective, cover texture, pages, bookmarks, all background pixels and square framing. Keep the cover fully CLOSED. The thin strap currently crossing slightly below the middle is lifted outward off the front cover: its right end rises a little toward the upper-right, creating a small believable loop and a narrow separated shadow on the cover; the left section still touches the cover. This is a subtle action, not a giant handle. Same strap material and thin width, no new buckle, clasp, hand, symbol or object. Match the original fine pixel-art rendering exactly. Keep the exterior flat #1c1916. One frame only, not a sheet.

Strap-free prompt:

> Make the SECOND strap-release frame for this exact closed notebook, just before its cover opens. Change ONLY the leather elastic strap. Preserve exactly the book's closed silhouette, camera, position, scale, ivory pages, bookmarks, leather texture, fine pixel style and square framing. The thin horizontal strap is now completely slipped off the front cover to the RIGHT: no horizontal band remains on the face. Show the same thin brown strap loose beside the right edge as one modest curved loop hanging down along that edge, still attached behind the book. The loop should be visible but small, no handle, buckle or clasp; the notebook remains firmly CLOSED. The freed leather front is continuous with no dark horizontal stripe or old strap shadow. No hands, text or extra objects. Exterior background uniformly #1c1916. Return only this one animation frame.

Open-state prompt:

> Edit target: the attached Agent Note brown leather notebook illustration. Create its FULLY OPEN state for a restrained three-frame opening/closing animation. Preserve the exact warm leather, fine pixel-art illustration texture, ivory page layering, olive green and muted red bookmark colors, warm light from upper left, and matte warm-dark background #1c1916. Same notebook, viewed from slightly above at a restrained three-quarter angle: an open two-page spread, gently curved blank ivory pages and a fine central gutter, thin caramel leather cover visible beneath, no writing, symbols, pen or decoration. Elastic is released and tucked at the outer edge, never across the open pages. Keep the book centered on a square canvas, fit its entire OPEN spread within 78 percent of canvas width with generous margins, no cropped edges. Crisp stepped pixel clusters and subtle material shading, no heavy outlines or dramatic perspective. Entire background MUST be uniform RGB(28,25,22), no checkerboard, transparency, gradient or vignette. Output only this ONE open notebook, no UI, text, extra objects or sheet of variants. It should look like the same physical notebook opened, quiet and finely crafted.

Half-open-state prompt:

> Create the SINGLE HALF-OPEN transition frame between reference 1 (closed brown leather notebook) and reference 2 (fully open notebook). Preserve the exact material style, warm brown leather, ivory paper, green and red bookmarks, upper-left lighting and crisp fine pixel clusters. Camera stays fixed as in the open reference: near frontal viewed slightly from above, no dramatic isometric rotation. The front cover is halfway swung left around its spine, about 70 degrees open, showing a narrow foreshortened left inside cover/page and the right blank page block; the book has intermediate width between closed and fully open. Keep the notebook centered in the SAME square canvas with generous margins and approximately 60 percent canvas height, so its scale matches the fully open reference's page height. Elastic released, no strap across paper. No writing, symbols, other objects, labels or UI. Entire exterior background uniform solid #1c1916, no transparency, checkerboard, gradients or vignette. One elegant transition frame only, not a sprite sheet.

## Original closed state

Created with the built-in image generation tool from the user's brown leather notebook reference. PixelLab's asset workflow was consulted, but PixelLab was not connected and did not generate this image.

`notebook-source.png` preserves the final generated image. `notebook.png` is its 128 × 128 PNG export, resized with BOX sampling. Native terminal image protocols display this file; the CLI does not redraw it as character blocks. The generated source is an illustration, not a guaranteed hand-aligned 64 × 64 sprite.

Generation prompt:

> Create ONE beautifully crafted pixel-art product sprite for Agent Note, using the attached leather notebook as the subject reference. This is a style transfer from the reference, not a UI screenshot. Preserve the book's elegant upright portrait proportions, gently rounded leather cover, subtle top-and-right ivory page block, a tiny olive-green bookmark at top right and muted red tab on right, and the single THIN curved leather elastic running horizontally slightly below the middle. A closed personal notebook, no lock, no buckle, no label, no lettering. View: restrained three-quarter front view, front cover dominant, slightly see top and right pages, NOT exaggerated isometric. Professional hand-placed 64x64 pixel-art inventory icon quality: refined clustered pixels, carefully stepped curves, coherent warm umber-to-caramel ramps, crisp selective dark outlines, thin highlight at upper-left leather edge, delicate mottled leather suggested by sparse 1-pixel texture, beautifully layered paper lines, clear contact shadows. Aim for the crafted quality of a premium indie game's item sprite. Rich material depth and subtle hue shifting; no flat slabs, no crude ASCII blocks, no checkerboard dithering, no rectangular polka dots, no blurry photographic noise, no vector look, no shiny plastic. Output a square image that is a PERFECT nearest-neighbor upscale of a single 64x64 sprite, so all pixel edges align to an even fine grid and remain crisp. The notebook occupies about 78 percent of image height with generous equal padding. Genuinely transparent RGBA background, no cream rounded-square app-icon container, no backdrop, no text or watermark. Only the notebook. Warm natural lighting from upper left. It must read cleanly as an elegant leather notebook even at small icon size.

Background correction prompt (the first output contained a visible checkerboard):

> Edit ONLY the background of this pixel-art notebook asset. Preserve the notebook's silhouette, colors, exact orientation, leather texture, book pages, fine horizontal leather elastic, green and red bookmarks and all pixel clusters. Replace EVERY white/gray checkerboard area outside the notebook with one perfectly uniform flat matte color RGB(28,25,22), hex #1c1916. The checkerboard is NOT wanted. No transparency and absolutely no checkerboard. No gradient, no vignette, no ambient lighting or decoration in the background. The entire exterior canvas including every corner must be the same #1c1916. Keep the notebook as-is and sharply pixelated. Preserve square framing and padding. Return just this single sprite on the uniform dark warm background.
